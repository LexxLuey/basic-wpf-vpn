<Window x:Class="KeenVPN.Presentation.Views.SignInWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign In" Height="300" Width="400"
        ResizeMode="CanMinimize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Email" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
        <TextBlock Text="Password" Grid.Row="2" Margin="0,10,0,5"/>
        <PasswordBox Grid.Row="3" Height="30" x:Name="PasswordBox" PasswordChar="*" PasswordChanged="PasswordBox_PasswordChanged"/>
        <Button Content="Sign In" Grid.Row="4" Height="35" Margin="0,20,0,0" Command="{Binding SignInCommand}"/>
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Grid.Row="5" Margin="0,10,0,0"/>
    </Grid>
</Window>
